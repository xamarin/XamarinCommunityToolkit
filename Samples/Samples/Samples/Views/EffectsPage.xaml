<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Xamarin.Toolkit.Samples.Views.EffectsPage"
             Title="Effects">
    <Grid HorizontalOptions="CenterAndExpand" VerticalOptions="CenterAndExpand">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Button Text="Label effects"
                Clicked="OnLabelButtonClicked"
                Grid.Row="0" />

        <Button Text="Entry effects"
                Clicked="OnEntryButtonClicked"
                Grid.Row="1" />

        <Button Text="Switch effects"
                Clicked="OnSwitchButtonClicked"
                Grid.Row="2" />

        <Button Text="View effects"
                Clicked="OnViewButtonClicked"
                Grid.Row="3" />
    </Grid>
</ContentPage>